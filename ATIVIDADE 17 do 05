#msg= msg.replace (' ' ', \n')
#fazer usando while
#fazer usando for 
'eu moro em natal' digitar a frase e ele precisa sair cada palavra em uma linha sem usar .replace no for e while

'''usando for'''
msg= ' eu moro em natal'
key = 6
msg_cripto =''
for letra in msg:
    letra_asc = ord(letra)
    letra_asc += key
    letra_cripto=chr(letra_asc)
    msg_cripto += letra_cripto
    print(msg_cripto)


'''usando while'''
msg= msg.replace (' ' ', \n')
key = 6
posicao=0
msg_cripto=''
while posicao < len(msg):
    letra_asc = ord(msg[posicao])
    letra_asc += key
    letra_cripto=chr(letra_asc)
    msg_cripto += letra_cripto
    posicao +=1
    print(msg_cripto)




