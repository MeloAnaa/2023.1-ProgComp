pop()
 nome_da_lista.pop(indice)
 variavel = nome_da_lista.pop(indice)
 
 
remove()
 nome_da_lista.remove(valor)
 
del
 del nome_da_lista[indice]
clear()
 nome_da_lista.clear()
 
 lanche.insert(posicao_da_lista, elemento)
 
 if pizza in lanche:
  lanche.remove(pizza)
 
 
 valores=[,21,4,31,23,2]
 valores.sort      #ordena os valores da lista
