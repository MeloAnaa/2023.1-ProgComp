'''dado um valor inteiro positivo, fazer u, programa que exiba todos os divisores desse numero'''
'''valor= int(input("informe um valor:"))
if (valor <=0):
    print("erro")
else:
    divisor=1
    while divisor <= valor:
        if (valor % divisor ==0):
            print(divisor)
            divisor +=1 '''


'''o msm codigo so que agr ele temq dizer os numeros primos'''
valor= int(input("informe um valor:"))
if(valor<=0):
    print("erro")
else:
    gt_divisores = 0
    divisor = 1
    while divisor <= valor:
        if(valor%divisor==0):gt_divisores+=1
        if (gt_divisores >2):break
        divisor+=1
        if(gt_divisores==2):
            print("primo")
        else:
            print("nao primo")

    
